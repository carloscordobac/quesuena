# raw/

Aquí van los originales (ogg, wav, flac, mp3…) antes de procesarlos con `npm run ingest`.
Esta carpeta no se sube a Git (salvo este fichero). Estructura y ficha de cada sonido: ver `docs/CONTENIDO.md`.
